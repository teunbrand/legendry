% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide_grid_swap.R
\name{guide_grid_swap}
\alias{guide_grid_swap}
\title{Swapped grid guide}
\usage{
guide_grid_swap(swap = "xy", force_minor = TRUE)
}
\arguments{
\item{swap}{A \code{character(1)}, one of \code{"x"}, \code{"y"} or \code{"xy"} indicated the
grid directions to swap. \code{"xy"} (default) will swap both the x and y grids.}

\item{force_minor}{A \code{logical(1)}. If \code{TRUE} (default), regular minor breaks
are calculated when missing, which is often the case for discrete scales.
If \code{FALSE}, just give major grid a minor grid appearance.}
}
\value{
A \verb{<Guide>} ggproto object that can be given to \code{coord_guided()}.
}
\description{
This grid guide swaps the display of major and minor breaks. This may be
particularly handy for discrete scales, to draw the major grid as a separator
between categorical values, instead of at the center.
}
\examples{
# A standard plot
p <- ggplot(mpg, aes(class)) +
  geom_bar()

# Swapping major grid to minor positions
p + coord_guided("grid_swap")

# Only swap x-grid, leave y-grid as-is
p + coord_guided(guide_grid_swap("x"))

# Just display major as minor, don't make new minor when missing
p + coord_guided(guide_grid_swap(force_minor = FALSE))
}
\seealso{
Other grid variants: 
\code{\link{guide_grid_vanilla}()},
\code{\link{guide_grid_zebra}()}
}
\concept{grid variants}
