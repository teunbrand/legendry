% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide_grid.R
\name{breaks_between}
\alias{breaks_between}
\title{Breaks between discrete categories}
\usage{
breaks_between(extremes = TRUE)
}
\arguments{
\item{extremes}{A \code{logical(1)} whether breaks should be placed before and
after the first and last category. If \code{TRUE}, these are returned.
If \code{FALSE}, these are omitted.}
}
\value{
A \code{function} that can be passed to grid guides.
}
\description{
This is a function factory that returns a function for placing breaks in
between discrete categories for use in grid guides.
}
\note{
This function should \emph{not} be used for \verb{scale_\{aes\}_\{type\}(breaks = ...)}
input.
}
\examples{
# By default, before and after breaks are included
breaks_between()(c("A", "B", "C"))

# Only between categories
breaks_between(FALSE)(c("A", "B", "C"))

# Continuous input returns waiver
breaks_between()(1:3)
}
