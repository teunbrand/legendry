% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-legend-manual.R
\name{guide_legend_manual}
\alias{guide_legend_manual}
\title{Manual legend}
\usage{
guide_legend_manual(
  labels,
  ...,
  layers = list(geom_point()),
  title = NULL,
  legend_args = list()
)
}
\arguments{
\item{labels}{Labels to display next to the keys. Can be a \verb{<character>} or \verb{<expression>}
vector to set labels, or \code{NULL} to draw no labels.}

\item{...}{Arguments interpreted as aesthetics. For example: \code{colour = "red"}.
The aesthetics must have the same size as the \code{labels} argument, or have
size 1. These aesthetics may be overruled by fixed aesthetics set in the
\code{layers} argument.}

\item{layers}{A \verb{<list>} of layers (\verb{<LayerInstance>} objects) created by the \verb{geom_*()} or
\verb{stat_*()} family of functions. These layers are used for their
\code{\link[ggplot2:draw_key]{key_glyph}} drawing functions, as well as to populate
default aesthetics. Any fixed aesthetics provided to these layers overrule
aesthetics passed to the \code{...} argument.}

\item{title}{One of the following to indicate the title of the guide:
\itemize{
\item A \verb{<character[1]>} or \verb{<expression[1]>} to set a custom title.
\item \code{NULL} to not display any title.
}}

\item{legend_args}{A \verb{<list>} of arguments passed on to \code{\link[=guide_legend_base]{guide_legend_base()}}.}
}
\value{
A \verb{<GuideCustom>} object.
}
\description{
This is a guide that displays user-defined keys independent of scales.
It should only be used as a last resort when struggling to format a
conventional legend.
}
\details{
Because this guide is not tied to a scale, it can be given an arbitrary name
in \code{guides()}; as long as it doesn't clash with other aesthetics.
}
\examples{
# A standard plot
p <- ggplot(mtcars, aes(disp, mpg)) +
  geom_point()

# Typical usage: set `label` and some aesthetics
p + guides(
  some_name = guide_legend_manual(
    label  = c("foo", "bar"),
    colour = c(NA, "black"),
    fill   = c("grey40", NA),
    layers = geom_col()
  )
)

# Alternative: use `layers` to set aesthetics
p + guides(
  some_name = guide_legend_manual(
    label = c("foo", "bar"),
    layers = geom_col(
      # Must match length of `label`
      colour = c(NA, "black"),
      fill = c("grey40", NA)
    )
  )
)

# You can use >1 layer
p + guides(
  some_name = guide_legend_manual(
    label = c("foo", "bar"),
    colour = c("tomato", "dodgerblue"),
    fill = NA,
    layers = list(geom_col(), geom_point())
  )
)
}
\seealso{
Other standalone guides: 
\code{\link{guide_axis_base}()},
\code{\link{guide_axis_dendro}()},
\code{\link{guide_axis_nested}()},
\code{\link{guide_axis_plot}()},
\code{\link{guide_circles}()},
\code{\link{guide_colbar}()},
\code{\link{guide_colring}()},
\code{\link{guide_colsteps}()},
\code{\link{guide_legend_base}()},
\code{\link{guide_legend_cross}()},
\code{\link{guide_legend_group}()}

Other legend guides: 
\code{\link{guide_legend_base}()},
\code{\link{guide_legend_cross}()},
\code{\link{guide_legend_group}()}
}
\concept{legend guides}
\concept{standalone guides}
